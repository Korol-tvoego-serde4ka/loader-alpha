<Window x:Class="MinecraftLoader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Minecraft Loader" Height="450" Width="800"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Dark" PrimaryColor="DeepPurple" SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <materialDesign:Card Grid.Row="0" Margin="16" Padding="16">
            <StackPanel>
                <TextBlock Text="Minecraft Loader" 
                         Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                         HorizontalAlignment="Center"/>
                <TextBlock Text="Введите ваш ключ для запуска" 
                         Style="{StaticResource MaterialDesignBody1TextBlock}"
                         HorizontalAlignment="Center"
                         Margin="0,8,0,0"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Основной контент -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Поле ввода ключа -->
            <TextBox x:Name="KeyTextBox"
                     Grid.Row="0"
                     materialDesign:HintAssist.Hint="Введите ключ"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Margin="0,0,0,16"
                     TextChanged="KeyTextBox_TextChanged"/>

            <!-- Кнопка запуска -->
            <Button x:Name="LaunchButton"
                    Grid.Row="1"
                    Content="Запустить"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="LaunchButton_Click"
                    IsEnabled="False"
                    Margin="0,0,0,16"/>

            <!-- Статус -->
            <TextBlock x:Name="StatusText"
                      Grid.Row="2"
                      Text="Введите ключ для продолжения"
                      Style="{StaticResource MaterialDesignBody1TextBlock}"
                      VerticalAlignment="Top"
                      HorizontalAlignment="Center"/>
        </Grid>

        <!-- Футер -->
        <materialDesign:Card Grid.Row="2" Margin="16" Padding="8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="© 2024 Minecraft Loader" 
                         Style="{StaticResource MaterialDesignBody2TextBlock}"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window> 